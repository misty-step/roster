---
name: {{binary}}
description: |
  HARD PLACEHOLDER: replace with 2-4 concrete {{project}} agent-task trigger
  branches, using product language an agent or operator will actually type.
  Include explicit Use when phrases and Trigger: /{{binary}}.
---

# {{project}}

{{description}}

## Agent Tasks

HARD PLACEHOLDER: replace this section with the real agent tasks this skill
should make predictable. Do not keep CLI/API/MCP procedure here.

For each task, name:

- intent: the user-visible job the agent is trying to finish
- surface: exact CLI command, API route, MCP tool, SDK call, or UI path
- proof: artifact, output, replay, trace, or gate that proves the job worked
- stop: approval, mutation, spend, or production boundary

## Proof

HARD PLACEHOLDER: replace with the product's acceptance oracle. A valid proof
names exact commands/routes/tools, expected evidence, and residual risk. Generated
API docs, SDK docs, or `--help` output do not prove the product worked.

## Safety

HARD PLACEHOLDER: replace with concrete read/write/destructive boundaries for
{{project}}. Split read-only, write, destructive, spend, production, and secret
handling rules where they differ. If no boundary exists for a class, say so and
why.
