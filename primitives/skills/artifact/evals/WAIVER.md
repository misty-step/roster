# /artifact eval waiver

expires: 2026-08-30

## Reason

Shipped 2026-06-30 (PR #138); no usage history yet to calibrate fixtures or a rubric against. Needs at least one real invocation's output before an eval can distinguish a good report from a mediocre one.

## Disposition

Not exempt from the eval-coverage contract — this waiver is a time-boxed
deferral, not a permanent opt-out. When it expires, either an eval spec lands
at `primitives/skills/artifact/evals/artifact-eval.md` (see `primitives/skills/skill-eval/templates/eval-spec.md`)
or this waiver is renewed with a fresh reason and date. A stale, silently
renewed waiver with no new reasoning is itself a finding for `/harness-engineering`'s
next skill-health audit.

Proof runs through the repository's standard skill-eval process.
