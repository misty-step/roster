# Estate bounded reversible request

This role requests only `restart` or `deploy_release` actions. The declaration
is descriptive, not runtime identity or approval: Estate must still verify the
exact artifact, resource and consequence bounds, expiry, authorization basis,
payload-schema eligibility, and runtime-key proof before any executor acts.
